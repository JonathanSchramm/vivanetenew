
                <!-- {% if user.is_authenticated %}
                Links disponíveis apenas para usuários autenticados
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'lista_clientes' %}">Clientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'lista_pagamentos' %}">Pagamentos</a>
                </li>
                <li class="nav-item">
                    <form method="post" action="{% url 'logout' %}" style="display: inline;">
                        {% csrf_token %}
                        <button type="submit" class="btn btn-link nav-link" style="background: black;">
                            Sair
                        </button>
                    </form>
                </li>
                {% else %}
                Links disponíveis apenas para usuários não autenticados
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'registro' %}">Registrar</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'login' %}" style="background: black; border-radius: .25rem; text-align: center;">Entrar</a>
                </li>
                {% endif %} -->